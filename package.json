{
  "name": "github-action-standalone-stats",
  "version": "1.0.0",
  "description": "GitHub Action that will publish coverage, test results, and other stats to gh-pages for self-contained pretty charts and badges. PLACEHOLDER — the publishing implementation is TODO; the action currently just logs its inputs.",
  "private": true,
  "type": "module",
  "author": "Cameron Tacklind <cameron@tacklind.com>",
  "license": "MIT",
  "keywords": [
    "github-actions",
    "stats",
    "coverage",
    "gh-pages"
  ],
  "engines": {
    "bun": ">=1.2"
  },
  "scripts": {
    "build": "bun build src/main.ts --target=node --format=cjs --outfile=dist/index.js",
    "lint": "tsc --noEmit",
    "test": "bun test src",
    "format": "prettier --write ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cinderblock/github-action-standalone-stats.git"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/node": "^22.19.19",
    "prettier": "^3.5.0",
    "typescript": "^6.0.3"
  }
}
